# Copier vers deploy/board.env sur le board (pas versionné) et adapter.
# 'export' est nécessaire : ce fichier est sourcé par run_pipeline.sh, et sans export
# la variable resterait locale au script shell, invisible pour le sous-processus python3.
export DASHBOARD_BASE_URL=http://192.168.1.XX
